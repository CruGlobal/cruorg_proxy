version: 2

updates:
  # Go modules. Minor and patch bumps are grouped into one weekly PR; majors
  # stay ungrouped so each gets its own PR.
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      go-dependencies:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    commit-message:
      prefix: "deps"
      include: "scope"

  # GitHub Actions used by .github/workflows/**.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 3
    groups:
      github-actions:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    commit-message:
      prefix: "ci"
      include: "scope"
    ignore:
      # The parked v1 workflow pins CruGlobal/.github at v1; v2 is a different
      # pipeline, so a major bump there would break it.
      - dependency-name: "CruGlobal/.github*"
        update-types: ["version-update:semver-major"]

  # Base images in the Dockerfile (literal tags, so Dependabot can read them).
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "build"
      include: "scope"
